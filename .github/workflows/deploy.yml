name: Demo CI

on:
  push:
    branches: [ "main" ]

jobs:
  first_job:
    runs-on: ubuntu-latest

    steps:
      - name: Run echo
        run: echo "this is first job"
  second_job:
    runs-on: ubuntu-latest 1.0

    steps:
      - name: Run echo
        run: echo "this is second job"